# ============================================================
# SIET Academic Background Verification Portal – Backend
# Python 3.13+ | FastAPI
# ============================================================

# ----- Core Framework -----
fastapi==0.115.12
uvicorn[standard]==0.34.3

# ----- Settings / Configuration -----
pydantic-settings==2.9.1
pydantic[email]==2.11.5
python-dotenv==1.1.0

# ----- Security -----
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.5.0
cryptography==44.0.3

# ----- Database (async SQLAlchemy – wires up to Parthiban's PostgreSQL) -----
sqlalchemy==2.0.41
asyncpg==0.30.0
alembic==1.16.1

# ----- HTTP Client (for payment provider server-side verification) -----
httpx==0.28.1

# ----- Email (SMTP – stub in Sprint 1, real SMTP in Sprint 2+) -----
aiosmtplib==3.0.2
jinja2==3.1.6

# ----- Testing -----
pytest==8.4.1
pytest-asyncio==0.24.0
httpx==0.28.1
